# Leave empty for local development through the Vite /api proxy.
# GitHub Pages leaves this empty because it cannot host the optional Function.
# Set it only when a separately hosted Function origin and CORS policy are ready.
VITE_API_BASE_URL=
